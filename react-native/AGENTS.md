# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# React Native Testing Library

This project uses `@testing-library/react-native`. Before writing or changing
React Native Testing Library tests, read the relevant local guidance in
`node_modules/@testing-library/react-native/docs/`, beginning with
`guides/llm-guidelines.md`. Prefer role- and label-based queries and
`userEvent` for user interactions.
