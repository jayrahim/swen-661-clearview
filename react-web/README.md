# ClearView React web

This directory is the React/Vite starter reserved for future course work. The
active ClearView mobile prototypes are in `../flutter` and `../react-native`.

## Run

```sh
npm install
npm run dev
```

Build or lint the starter with:

```sh
npm run build
npm run lint
```
